
// tokyo_tower ALL Names

switch (FEAT_HOUSES, SELF, name_tokyo_tower, random_bits % 2 ) { // Ref functions.CreateNameSwitches() 
	0: 	return string(NAME_TOKYO_TOWER);
	1: 	return string(NAME_SAPPORO_TV_TOWER);
}
