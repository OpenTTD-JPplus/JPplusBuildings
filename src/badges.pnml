
/* 
badgetable {
    // Zones 
    "zone",
    "zone/religion",
}

item (FEAT_BADGES, zone) {
  property {
   label:     "zone";
   name:      string(STR_ZONE);
  }
}

item (FEAT_BADGES, religion) {
  property {
   label:     "zone/religion";
   name:      string(STR_ZONE_RELIGION);
  }
}
*/