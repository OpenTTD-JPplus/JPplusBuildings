/* Check for road (and stations) in adjacent tiles functions */
switch (FEAT_HOUSES, SELF, NearbyTileHouseIDNE, houseid, nearby_tile_class(-1, 0) == houseid ) {return;}
//switch (FEAT_HOUSES, SELF, NearbyTileHouseIDNW, nearby_tile_class( 0,-1) ) {return;}
//switch (FEAT_HOUSES, SELF, NearbyTileHouseIDSE, nearby_tile_class( 0, 1) ) {return;}
switch (FEAT_HOUSES, SELF, NearbyTileHouseIDSW, houseid, nearby_tile_class( 1, 0) == houseid ) {return;}
