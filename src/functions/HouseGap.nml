/* Begin HouseGap */

switch(FEAT_HOUSES, SELF, HouseOfficeGap,
	
	// MAIN CRITERIA - Houses and Townhouses cannot build within two tiles of Commercial Buildings (Classes 2 & 5)
	!CommercialWithinTwoTiles()

	||

	// EXCEPTION - Unless there is a commercial building two tiles away (but not adjacent i.e. 1 tile) AND NO passenger service
	( CommercialWithinTwoTiles() && !CommercialWithinOneTile() && !FuncServicePASS() )

){return;}

/* End HouseGap */
