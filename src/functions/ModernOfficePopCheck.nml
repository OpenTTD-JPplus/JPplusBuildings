/* Begin Modern Office Population Check */

// This function compares the total of the Population Adder Function to the formula below
// It should Return 1 when the residential factor exceeds the commercial factor

switch (FEAT_HOUSES, SELF, ModernOfficePopCheck, 
	(FuncPopulationAdder() > ( (other_class_count_town(3) * 30) + ((1 + same_class_count_town) * 60) ) ) 
) {return;}

/* End Modern Office Population Check */
